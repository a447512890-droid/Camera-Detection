# 原型导航与需求说明复用规范

## 1. 文档用途

本规范提取自当前 HTML 原型，用于其他项目快速复用以下两套能力：

1. 左侧项目树导航：用于组织更新日志、需求清单、流程图和产品原型页面。
2. 原型模块需求说明：每个小模块旁显示动态圆点，点击后在原型外部的右侧说明栏展示详细需求，不遮挡、不模糊原型。

这套结构适用于产品原型评审、需求走查、双语界面对比和交互说明交付。复制到新项目时，应替换项目名称、目录数据、页面选择器和需求内容，不应直接保留示例业务数据。

---

## 2. 整体结构

桌面端采用三列网格：

| 区域 | 默认宽度 | 作用 |
| --- | ---: | --- |
| 左侧项目树 | 264px | 项目目录、搜索、折叠与页面跳转 |
| 中间内容区 | `minmax(430px, 1fr)` | 双语原型或项目文档 |
| 右侧需求说明栏 | 312px | 点击需求点后显示模块说明，默认隐藏 |

右侧说明栏关闭时，仅保留前两列。打开说明栏时，在工作区增加第三列，不使用遮罩层或模态框。

```html
<main class="workspace">
  <aside class="project-panel">...</aside>
  <section class="doc-viewer">...</section>
  <div class="prototype-comparison">...</div>
  <aside class="requirement-inspector" hidden>...</aside>
</main>
```

核心布局：

```css
.workspace {
  min-height: 100dvh;
  display: grid;
  grid-template-columns: 264px minmax(430px, 1fr);
}

.workspace.has-requirement-inspector {
  min-width: 1006px;
  grid-template-columns: 264px minmax(430px, 1fr) 312px;
}
```

---

## 3. 共用视觉变量

建议新项目保留变量结构，再按品牌替换数值：

```css
:root {
  --bg: #eef7f8;
  --bg-deep: #dff2f3;
  --surface: rgba(255, 255, 255, .92);
  --surface-solid: #fff;
  --surface-soft: #f3f8f8;
  --text: #112d32;
  --text-2: #516c71;
  --text-3: #82979b;
  --line: rgba(31, 92, 101, .11);
  --brand: #08a8b5;
  --brand-deep: #047d89;
  --brand-soft: #dff7f8;
  --blue: #3f7cf4;
  --warning: #e49b2f;
  --danger: #d95c6b;
  --success: #18a979;
  --shadow-sm: 0 8px 24px rgba(23, 68, 74, .08);
  --shadow-lg: 0 30px 80px rgba(19, 64, 71, .18);
  --radius-sm: 12px;
  --radius: 20px;
  --radius-lg: 28px;
  --ease: cubic-bezier(.2, .8, .2, 1);
}
```

字体优先使用系统无衬线字体，中文项目需包含 `PingFang SC` 和 `Microsoft YaHei` 回退。

---

## 4. 左侧项目树规范

### 4.1 信息结构

推荐层级：

```text
项目名称
├── 需求清单
│   ├── 更新日志
│   ├── 需求列表 vX.X.X
│   └── 需求列表 vX.X.X
└── 版本号
    ├── 流程图
    │   ├── 流程图 A
    │   └── 流程图 B
    └── 产品原型
        ├── 页面 A
        ├── 页面 B
        └── 页面组
            └── 子页面
```

树结构只承担导航与层级展示，不在导航区域放置页面业务内容。

### 4.2 页面结构

```html
<aside class="project-panel" aria-label="项目目录">
  <div class="project-header">
    <span class="project-emblem" aria-hidden="true">...</span>
    <div class="project-name">
      <span class="project-kicker">Product map</span>
      <div class="project-title">项目名称</div>
    </div>
    <div class="project-nav" aria-label="浏览历史">
      <button type="button" aria-label="上一个页面">...</button>
      <button type="button" aria-label="下一个页面">...</button>
    </div>
  </div>

  <div class="tree-search">
    <svg aria-hidden="true">...</svg>
    <input type="search" placeholder="Search" aria-label="搜索项目页面">
  </div>

  <nav class="project-tree" aria-label="原型页面目录">
    <button class="tree-row" style="--depth:0" data-tree-toggle="requirements" aria-expanded="true">...</button>
    <div class="tree-group" data-tree-group="requirements">...</div>
  </nav>
</aside>
```

### 4.3 缩进方式

每个节点通过 `--depth` 控制缩进，不为不同层级重复创建 CSS 类：

```css
.tree-row {
  padding-left: calc(9px + var(--depth, 0) * 14px);
}
```

建议：

- 根节点使用 `--depth:0`。
- 一级节点使用 `--depth:1`。
- 二级节点使用 `--depth:2`。
- 三级节点使用 `--depth:3`。
- 层级不宜超过四级；更深层级会降低扫描效率并压缩文字空间。

### 4.4 视觉规则

- 面板固定宽度：264px。
- 顶部品牌区高度：82px。
- 搜索框高度：42px，圆角 14px。
- 普通树节点最小高度：36px。
- 分组节点最小高度：40px。
- 节点圆角：10px。
- 选中状态同时使用浅色背景、左侧品牌色指示条和加粗文字，不能只依赖颜色。
- 长标题使用单行省略号，完整名称应保留在可访问名称或 `title` 中。
- 图标统一使用同一套 SVG 线性图标，避免混用 Emoji。

当前颜色语义：

| 类型 | 颜色 |
| --- | --- |
| 需求清单 | 紫色 |
| 版本分组 | 青色 |
| 流程图 | 橙色 |
| 产品原型 | 蓝色 |
| 当前选中 | 品牌青绿色 |

### 4.5 交互规则

#### 折叠与展开

- 分组按钮使用 `aria-expanded="true|false"`。
- 按钮的 `data-tree-toggle` 必须与目标容器的 `data-tree-group` 一致。
- 收起时为目标容器设置 `hidden`。
- 箭头随 `aria-expanded` 状态旋转。

```js
const toggle = event.target.closest('[data-tree-toggle]');
if (toggle) {
  const group = document.querySelector(
    `[data-tree-group="${toggle.dataset.treeToggle}"]`
  );
  const expanded = toggle.getAttribute('aria-expanded') !== 'false';
  toggle.setAttribute('aria-expanded', String(!expanded));
  if (group) group.hidden = expanded;
}
```

#### 搜索

- 搜索范围为所有树节点的可见文字。
- 搜索时自动展开命中的父级路径。
- 清空搜索后恢复用户原来的展开状态。
- 搜索仅过滤导航，不修改页面业务数据。

#### 选中状态

- 任一时刻只保留一个 `.selected` 节点。
- 页面跳转成功后再更新选中状态。
- 选中节点应自动滚动到树面板可见范围内。

---

## 5. 模块需求说明规范

### 5.1 核心原则

需求说明必须按页面内的小模块拆分，不使用一条说明概括整个页面。

示例拆分：

- 搜索页：输入框、最近搜索、查询须知分别设置需求点。
- 结果页：结果摘要、治安、灾害、诈骗、文化分别设置需求点。
- 列表页：筛选器和列表主体分别设置需求点。
- 表单页：名称、快捷预设、上传区域分别设置需求点。

禁止把多个逻辑不同的区域强行合并成一个说明点。

### 5.2 默认状态

- 默认只显示青色小圆点。
- 详细说明默认隐藏。
- 点击圆点后，在原型外侧的右栏展示说明。
- 不使用遮罩层、不虚化原型、不阻止原型继续操作。
- 再次点击其他需求点时，复用同一个说明栏并更新内容。

### 5.3 需求说明字段

每个模块固定使用六个字段：

| 字段 | 内容要求 |
| --- | --- |
| 模块说明 | 说明该模块向用户提供什么能力 |
| 数据来源 | 指明数据来自本地配置、用户输入、系统能力、接口、模型字段或第三方服务 |
| 前置条件 | 说明模块可用前必须满足的状态、授权或数据条件 |
| 后置结果 | 说明用户操作后页面、数据或系统状态如何变化 |
| 显示数量限制 | 给出最大数量、单次数量、字符数、文件数、时间或“不适用” |
| 排序规则 | 明确固定顺序、时间倒序、距离优先、风险优先或“不适用” |

说明内容应是可验证的产品规则，避免宣传文案和模糊表述。

### 5.4 内容编写限制

- 一个需求点只描述一个模块。
- 不补写原需求中没有依据的关键字段、风险结论或业务流程。
- 推测性规则必须标记为“补充建议”，不能伪装为原始需求。
- 数据来源必须具体，不能只写“后台数据”。
- 前置条件应包含权限、登录、网络、已选状态或必要输入。
- 后置结果必须描述可观察结果，不能只写“处理成功”。
- 数量限制必须包含单位，例如“最多 20 条”“单张 20MB”“50 个字符”。
- 排序规则必须说明主排序和次排序，例如“高风险优先，其次距离由近到远”。
- 没有数量或排序含义时统一写“不适用”，不要留空。
- 可按项目决定启用哪些语言。当前参考项目只在中文原型生成需求点，英文原型不显示说明。
- 若同时启用中英文说明，两种语言必须表达同一规则，不允许两套规则不一致。

### 5.5 数据结构

推荐使用统一的双语数据模型：

```js
const bilingual = (en, zh) => [en, zh];

const moduleNote = (
  title,
  overview,
  source,
  pre,
  post,
  limit = 'Not applicable',
  limitZh = '不适用',
  sort = 'Not applicable',
  sortZh = '不适用'
) => ({
  title: bilingual(...title),
  overview: bilingual(...overview),
  source: bilingual(...source),
  pre: bilingual(...pre),
  post: bilingual(...post),
  limit: bilingual(limit, limitZh),
  sort: bilingual(sort, sortZh)
});

const moduleNoteCatalog = {
  searchInput: moduleNote(
    ['Destination input', '目的地输入'],
    ['Enter a city or region.', '输入城市或地区。'],
    ['User input.', '用户输入。'],
    ['The page has loaded.', '页面已加载。'],
    ['Use the destination in the query.', '将目的地用于查询。'],
    '1 destination',
    '1 个目的地'
  )
};
```

#### 5.5.1 外部 Markdown 数据源

需要让产品或需求人员直接维护说明时，可把中文字段放入同目录的 `需求说明.md`，页面加载后解析并覆盖 HTML 中的默认值：

```md
## homeRisk
- 标题: 目的地风险入口
- 模块说明: 进入目的地风险查询。
- 数据来源: 本地导航配置。
- 前置条件: 首页已加载。
- 后置结果: 进入风险查询页。
- 显示数量限制: 单个模块
- 排序规则: 不适用
```

约束：

- `##` 后的键必须与 `moduleNoteCatalog` 一致。
- 字段名不得修改或重复。
- 页面应通过 HTTP 或 HTTPS 地址打开；本地可使用静态服务器，线上可使用 GitHub Pages。`file://` 通常会因浏览器安全限制而无法读取相邻 Markdown。
- 使用 GitHub Pages 发布纯静态原型时，在发布目录根部保留空的 `.nojekyll` 文件，避免 Markdown 数据源被 Jekyll 转换。
- 请求应使用 `cache: 'no-store'` 或时间戳，确保刷新后读取最新内容。
- Markdown 缺失、格式错误或加载失败时，应回退到 HTML 内置说明。
- 外部 Markdown 只覆盖已识别的模块和字段，不应动态执行其中的 HTML 或 JavaScript。

### 5.6 页面与模块映射

不要把选择器写入需求数据本身。单独维护页面、选择器与需求键的映射表：

```js
const moduleNoteTargets = [
  ['search-page', '.search-box', 'searchInput'],
  ['search-page', '.recent-list', 'recentSearch'],
  ['result-page', '.result-summary', 'resultSummary']
];
```

字段顺序：

```text
[页面 data-screen, 模块选择器, moduleNoteCatalog 键]
```

选择器必须在对应页面内唯一。优先使用稳定的 `data-*` 属性或语义类名，避免依赖易变化的 `nth-child`。

### 5.7 需求点结构

需求点由 44×44px 的可点击区域和内部小圆点组成：

```css
.module-note-wrap {
  position: relative;
  min-width: 0;
}

.module-note-dot {
  position: absolute;
  z-index: 28;
  top: -10px;
  right: -10px;
  width: 44px;
  height: 44px;
  border: 0;
  border-radius: 50%;
  background: transparent;
  cursor: pointer;
}

.module-note-dot::after {
  content: "";
  position: absolute;
  inset: 14px;
  border: 2px solid #fff;
  border-radius: 50%;
  background: var(--brand);
  box-shadow: 0 3px 10px rgba(8,125,137,.34);
}

.module-note-dot::before {
  content: "";
  position: absolute;
  inset: 12px;
  border: 1.5px solid rgba(8,168,181,.65);
  border-radius: 50%;
  animation: moduleNoteRipple 1.8s ease-out infinite;
}

@keyframes moduleNoteRipple {
  0% { transform: scale(.6); opacity: .85; }
  75%, 100% { transform: scale(2.1); opacity: 0; }
}
```

动态圆点只能承担“查看说明”操作，不能与模块的主要操作共用同一个按钮。

### 5.8 自动挂载方式

为避免出现按钮嵌套按钮，需求点必须作为业务模块的兄弟元素，而不是子元素：

```js
function addModuleNotes() {
  document.querySelectorAll('.screen').forEach(screen => {
    // 当前参考项目仅在中文原型挂载说明点；其他项目可删除或修改此条件。
    if (screen.closest('[data-locale]')?.dataset.locale !== 'zh') return;

    moduleNoteTargets
      .filter(([page]) => page === screen.dataset.screen)
      .forEach(([, selector, key]) => {
        const target = screen.querySelector(selector);
        if (!target) return;

        let wrapper = target.parentElement?.classList.contains('module-note-wrap')
          ? target.parentElement
          : null;

        if (!wrapper) {
          wrapper = document.createElement('div');
          wrapper.className = 'module-note-wrap';
          target.before(wrapper);
          wrapper.appendChild(target);
        }

        if (wrapper.querySelector(`:scope > [data-module-note="${key}"]`)) return;

        const button = document.createElement('button');
        button.type = 'button';
        button.className = 'module-note-dot';
        button.dataset.moduleNote = key;
        button.setAttribute('aria-expanded', 'false');
        button.setAttribute('aria-controls', 'requirementPanel');
        button.setAttribute('aria-label', `查看${moduleNoteCatalog[key].title[1]}需求说明`);
        wrapper.appendChild(button);
      });
  });
}
```

若目标是网格中的跨列元素，应把跨列类同步到 `.module-note-wrap`，否则包裹后可能破坏原网格布局。

---

## 6. 外置需求说明栏

### 6.1 结构

说明栏使用普通 `aside`，不是 `dialog`：

```html
<aside
  class="requirement-inspector"
  id="requirementPanel"
  aria-labelledby="requirementPanelTitle"
  aria-live="polite"
  hidden
>
  <header>
    <div class="requirement-inspector-heading">
      <span class="requirement-inspector-kicker">模块需求说明</span>
      <h2 id="requirementPanelTitle">模块名称</h2>
    </div>
    <button type="button" data-close-requirement aria-label="关闭需求说明">...</button>
  </header>
  <div class="requirement-detail-grid" id="requirementDetailGrid"></div>
</aside>
```

不得添加以下模态行为：

- 不添加全屏遮罩。
- 不设置 `aria-modal="true"`。
- 不模糊或降低原型透明度。
- 不锁定中间原型的点击、滚动和键盘操作。
- 不强制把键盘焦点困在说明栏中。

### 6.2 视觉规则

```css
.requirement-inspector {
  grid-column: 3;
  grid-row: 1;
  align-self: stretch;
  position: sticky;
  top: 0;
  width: 312px;
  height: 100dvh;
  overflow: hidden auto;
  padding: 22px 18px 30px;
  background: rgba(250,253,253,.98);
  border-left: 1px solid rgba(20,105,108,.14);
  box-shadow: -14px 0 36px rgba(25,68,73,.07);
}

.requirement-inspector[hidden] {
  display: none;
}

.requirement-detail-grid {
  display: grid;
  gap: 10px;
}

.requirement-detail {
  padding: 13px 15px;
  border-radius: 14px;
  background: #f5f9fa;
}
```

标题栏保持粘性，说明内容较长时只滚动右侧说明栏。

### 6.3 打开、切换与关闭

```js
let activeRequirementTrigger = null;

function setRequirementPanelOpen(trigger) {
  if (activeRequirementTrigger && activeRequirementTrigger !== trigger) {
    activeRequirementTrigger.setAttribute('aria-expanded', 'false');
  }

  activeRequirementTrigger = trigger;
  trigger.setAttribute('aria-expanded', 'true');
  requirementPanel.hidden = false;
  workspace.classList.add('has-requirement-inspector');
}

function closeRequirementPanel(restoreFocus = true) {
  if (requirementPanel.hidden) return;

  requirementPanel.hidden = true;
  workspace.classList.remove('has-requirement-inspector');

  if (activeRequirementTrigger) {
    activeRequirementTrigger.setAttribute('aria-expanded', 'false');
    if (restoreFocus) activeRequirementTrigger.focus();
  }

  activeRequirementTrigger = null;
}
```

交互要求：

- 点击新需求点时直接更新现有说明栏。
- 当前需求点使用 `aria-expanded="true"` 和蓝色选中态。
- 关闭说明栏后将焦点还给触发圆点。
- `Escape` 可关闭说明栏。
- 页面跳转时自动关闭说明栏，避免展示上一页的失效说明。

---

## 7. 响应式规则

### 大于 1100px

- 保留左侧项目树、中间原型、右侧说明栏三列。
- 双语原型可以同时显示。

### 701–1100px

- 说明栏打开时临时隐藏左侧项目树。
- 中间原型与右侧说明栏并排展示。
- 推荐列宽：`minmax(400px, 1fr) 280px`。
- 说明栏关闭后恢复左侧项目树。

```css
@media (min-width: 701px) and (max-width: 1100px) {
  .workspace.has-requirement-inspector {
    min-width: 0;
    grid-template-columns: minmax(400px, 1fr) 280px;
  }

  .workspace.has-requirement-inspector .project-panel {
    display: none;
  }
}
```

### 不大于 700px

- 工作区改为单列。
- 隐藏左侧项目树。
- 当前实现只展示一个语言版本的手机原型。
- 说明栏进入普通文档流，位于原型后方。
- 移动端若需要同时查看原型与说明，建议另行实现可调高度的上下分栏；不要重新使用遮罩模态框。

---

## 8. 可访问性要求

- 需求点的视觉圆点可以很小，但实际点击区域必须至少 44×44px。
- 所有纯图标按钮必须有 `aria-label`。
- 需求点使用 `aria-controls="requirementPanel"`。
- 需求点使用 `aria-expanded` 表示说明栏状态。
- 说明栏使用 `aria-live="polite"`，切换内容时不抢夺焦点。
- 关闭按钮必须可通过键盘访问。
- 键盘焦点样式不可移除。
- 颜色不能是唯一的状态提示；选中项还需使用边框、指示条、字重或形态变化。
- 波纹动画必须支持减少动态效果：

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: .01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: .01ms !important;
  }
}
```

---

## 9. 已知限制与注意事项

### 9.1 DOM 包裹会影响原布局

自动挂载需求点会为目标模块增加 `.module-note-wrap`。以下情况必须额外处理：

- CSS Grid 的跨列模块需要把 `grid-column` 同步到 wrapper。
- Flex 子项的 `flex`、`width` 或 `align-self` 可能需要转移到 wrapper。
- 使用 `:nth-child()` 的模块选择器会因 wrapper 插入而改变序号，优先改用稳定类名或 `data-*`。
- 目标模块本身为按钮时，需求点不能放进按钮内部，否则会形成非法的按钮嵌套。

### 9.2 动态列表

如果列表内容会通过 `innerHTML` 重绘，需求点应包裹列表容器，而不是列表内部的单条记录，否则重绘会删除需求点。

### 9.3 选择器唯一性

一个映射项当前只挂载到 `querySelector()` 找到的第一个元素。若页面有多个相同模块，应：

- 为每个模块提供唯一 `data-*`；或
- 将挂载逻辑改为 `querySelectorAll()` 并为每个实例生成唯一键。

### 9.4 超长说明

- 说明栏宽度有限，单个字段建议不超过 2–4 句。
- 复杂规则应拆分为多个模块点，不要把完整 PRD 塞进一个字段。
- 超长字段允许在右栏内部滚动，但不能撑高或遮挡原型区域。

### 9.5 小屏限制

小于 700px 时无法同时舒适展示手机原型和 280–312px 说明栏。当前策略优先保证原型可用，将说明栏放入后续文档流。若项目必须在手机上并排查看，应重新评估信息密度，而不是继续缩小文字。

### 9.6 状态同步

- 页面切换、文档切换或模块销毁时必须关闭旧说明。
- 当前激活需求点被删除前，应先清除 `activeRequirementTrigger`。
- 双语原型必须共用同一个说明栏，避免左右各生成一套面板造成布局拥挤。

### 9.7 安全与内容边界

- 需求说明中不得展示真实用户数据、访问令牌或生产接口凭据；若项目负责人明确要求展示密钥，只能使用已确认的演示凭据并清晰标注用途。
- 示例账号、设备名、地点和价格必须明确是原型数据。
- 外部数据源应描述来源类型，不应在说明栏中直接粘贴敏感响应内容。

---

## 10. 新项目接入步骤

1. 复制视觉变量和 `.workspace` 三列结构。
2. 创建左侧 `.project-panel`、搜索框和 `.project-tree`。
3. 用 `--depth` 建立目录层级，并实现折叠、搜索、跳转和选中状态。
4. 在每个原型页面上设置唯一的 `data-screen`。
5. 建立 `moduleNoteCatalog`，为每个小模块填写六项说明。
6. 建立 `moduleNoteTargets`，使用稳定选择器映射页面模块。
7. 执行 `addModuleNotes()`，检查没有按钮嵌套和网格错位。
8. 创建唯一的 `#requirementPanel` 外置说明栏。
9. 实现打开、切换、关闭、焦点恢复和页面跳转清理逻辑。
10. 检查桌面宽屏、中等宽度和移动端布局。

---

## 11. 验收清单

### 左侧导航

- [ ] 项目树层级、顺序和页面信息架构正确。
- [ ] 折叠、展开、搜索、选中和页面跳转可用。
- [ ] 当前页面只有一个选中项。
- [ ] 长标题不破坏布局。
- [ ] 图标风格和颜色语义一致。
- [ ] 键盘焦点清晰可见。

### 需求点

- [ ] 每个说明点只对应一个小模块。
- [ ] 默认只显示圆点，说明内容默认隐藏。
- [ ] 点击区域不少于 44×44px。
- [ ] 波纹不会遮挡模块文字或主要操作。
- [ ] 当前圆点有明确选中状态。
- [ ] 需求点只出现在项目指定的语言版本；当前参考项目仅启用中文原型。

### 外置说明栏

- [ ] 说明栏位于原型外部。
- [ ] 没有遮罩、背景模糊和原型交互锁定。
- [ ] 六个说明字段完整。
- [ ] 点击其他圆点可原位更新内容。
- [ ] 关闭后焦点回到触发点。
- [ ] 页面切换后不会保留旧说明。
- [ ] 内容过长时仅说明栏内部滚动。

### 响应式与无障碍

- [ ] 大屏可同时看到导航、原型和说明栏。
- [ ] 中等宽度打开说明栏时不会出现横向滚动。
- [ ] 小屏仍能访问原型和说明内容。
- [ ] `aria-label`、`aria-controls`、`aria-expanded` 状态正确。
- [ ] 支持 `Escape` 关闭。
- [ ] 支持 `prefers-reduced-motion`。

---

## 12. 当前参考实现

- HTML 原型：`摄像头监测原型/index.html`
- 本规范：`摄像头监测原型/原型导航与需求说明复用规范.md`

后续项目可以直接引用本规范，但必须重新建立项目自己的目录数据、模块映射和需求说明内容。
