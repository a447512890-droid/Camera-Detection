# 摄像头监测原型：项目交接与分窗口协作手册

> 最后整理：2026-09-17  
> 适用目录：`摄像头监测原型/`  
> 用途：作为后续所有新对话窗口的统一项目说明、执行边界和协作依据。  
> 当前实现真源：`index.html` + `需求说明.md`。`design-qa.md` 是历史变更记录，不能单独作为当前状态真源。

所有窗口还必须单独读取 `全局规则.md`；该文件是便于复制和引用的统一规则入口。本手册第 9 节保留规则摘要，若两处文字存在细节差异，以 `全局规则.md` 和最新用户要求为准。

---

## 1. 核心任务与建模目标

本项目的目标是把原始 Axure HTML 原型重新整理成一个可直接在浏览器打开、可交互、可展示、可通过 GitHub Pages 发布的单页静态原型。

核心交付目标：

1. 左侧保留类似 Axure 的项目树，用于访问更新日志、需求列表、流程图和产品原型页面。
2. 右侧同时展示英文和中文两套手机原型，便于对照。
3. 产品内容和字段尽可能忠实于 `原型文件/`，不得无依据扩写。
4. 英文和中文原型保持一致的页面与交互，但需求说明圆点只保留在中文原型。
5. 中文原型的模块需求说明从独立的 `需求说明.md` 加载，产品或需求人员可直接编辑 Markdown。
6. 三个流程图使用当前 HTML/CSS 原生绘制，不再引用原 Axure HTML，也不使用流程图内部滚动条。
7. 项目为纯静态页面，无构建步骤，可通过本地 HTTP 服务或 GitHub Pages 直接访问。

本项目不是正式 App、不是生产后端，也不是实际的大模型调用服务。当前交互主要用于产品原型演示。

---

## 2. 项目背景

### 2.1 原始资料

原始资料位于工作区：

```text
/Users/ruianyun/Nutstore Files/瑞安云/11_摄像头/
```

主要来源：

- `摄像头监测_iOS .rp`：Axure 工程源文件。
- `原型文件/`：Axure 导出的 HTML 页面，是产品字段、流程和文案的主要来源。
- `上架checklist.xlsx`：上架检查资料。
- `内购产品价格.xlsx`：会员与内购价格资料。
- 用户在历次对话中提供的标注截图：用于局部样式、内容删除和交互修订。

### 2.2 当前交付目录

```text
摄像头监测原型/
├── .nojekyll
├── App Store元数据.md
├── index.html
├── 需求说明.md
├── 原型导航与需求说明复用规范.md
├── 项目交接与分窗口协作手册.md
├── 跨窗口通知记录.md
├── design-qa.md
└── assets/
    └── icons/
        ├── PHOSPHOR-LICENSE.txt
        ├── desktop.svg
        ├── device-mobile.svg
        └── devices.svg
```

`.DS_Store` 属于 macOS 临时文件，不是交付内容，上传 Git 时应忽略。

### 2.3 当前访问地址

本地访问：

```text
http://localhost:8766/摄像头监测原型/
```

建议从工作区根目录启动静态服务：

```bash
python3 -m http.server 8766
```

不能把 `file://` 作为正式预览方式，因为浏览器通常不允许页面读取相邻的 `需求说明.md`。

GitHub Pages 发布后，访问形式通常为：

```text
https://<用户名>.github.io/<仓库名>/
```

---

## 3. 当前产品范围

### 3.1 左侧项目树

当前树状结构：

```text
需求清单
├── 更新日志
├── 需求列表 v1.0.1
└── 需求列表 v1.0.0

V1.0.0
├── 流程图
│   ├── 风险查询流程图
│   ├── 恢复购买流程
│   └── 订阅会员流程
└── 产品原型
    ├── 商店图
    ├── 首页
    │   ├── 目的地风险查询
    │   ├── 电子档案备份
    │   ├── 附近资源查询
    │   └── 住宿安全检测
    └── 我的
        ├── 注销账号
        └── 订阅会员
```

左侧项目树已经具备：

- 文件夹展开与折叠。
- 页面搜索。
- 当前项选中状态。
- 颜色分组、层级连接线和定制滚动条。
- 页面与文档跳转。

视觉结构和复用要求见 `原型导航与需求说明复用规范.md`。

### 3.2 产品原型页面

当前屏幕标识：

| 标识 | 页面 |
|---|---|
| `home` | 首页 |
| `risk` | 目的地风险输入 |
| `risk-loading` | 风险查询中 |
| `risk-result` | 风险查询结果 |
| `scan` | 住宿安全检测 |
| `scan-results` | 设备扫描结果 |
| `scan-detail` | 设备距离检测 |
| `resources` | 附近求助资源 |
| `vault` | 电子档案列表与管理 |
| `vault-add` | 新增电子档案 |
| `vault-preview` | 档案图片预览 |
| `recharge` | 会员订阅/充值 |
| `profile` | 我的 |
| `account-delete-read` | 注销说明与倒计时 |
| `account-delete-confirm` | 最终注销确认 |

### 3.3 项目文档页

| 标识 | 页面 |
|---|---|
| `changelog` | 更新日志 |
| `requirements-101` | 需求列表 v1.0.1 |
| `requirements-100` | 需求列表 v1.0.0 |
| `store-artwork` | 宣传图与引导图 |
| `flow-risk` | 风险查询流程图 |
| `flow-restore` | 恢复购买流程 |
| `flow-subscribe` | 订阅会员流程 |

URL 通过哈希定位，例如：

```text
#home
#profile
#doc-flow-risk
```

---

## 4. 数据来源、字段与数据样例

### 4.1 数据来源优先级

发生冲突时依次采用：

1. 用户在当前或最新对话中明确确认的要求。
2. 当前 `摄像头监测原型/index.html` 已实现且已确认的结果。
3. `原型文件/` 中的原始字段、流程和文案。
4. `需求说明.md` 中的中文模块说明。
5. `design-qa.md` 历史记录，仅用于追溯，不可覆盖新决定。

禁止仅根据旧截图或旧 QA 记录回滚当前已确认结果。

### 4.2 风险查询模型字段

原始风险查询模型固定返回：

```json
{
  "destination": "",
  "security_level": "",
  "natural_disaster_tip": "",
  "fraud_violence_tip": "",
  "culture_taboo": ""
}
```

字段含义：

- `destination`：用户输入的城市或区域。
- `security_level`：整体治安等级与说明。
- `natural_disaster_tip`：自然灾害类型、时段和出行提醒。
- `fraud_violence_tip`：诈骗、暴力及游客易遇场景。
- `culture_taboo`：当地习俗、宗教、着装和拍照注意事项。

失败约束：地点识别失败时保留 `destination`，其余字段返回空字符串。

### 4.3 当前模型方案

当前流程图记录的模型配置来自原始文件：

- 模型：Doubao-Seed-2.0-mini 260428
- Model ID：`doubao-seed-2-0-mini-260428`
- Temperature：`0.6`
- TopP：`0.8`
- 思考深度：`low`
- 接入点信息保留在原始 `原型文件/风险查询流程图.html` 中。
- 明文 API Key 当前也保留在 `index.html` 的风险流程图说明中。

重要说明：当前产品原型并未真正向模型接口发送请求。风险查询使用本地模拟加载与固定示例结果。

安全决策：用户已明确要求保留流程图中的明文 API Key，并在 GitHub Pages 准备阶段再次确认“不处理”。任何窗口不得自行隐藏、替换或删除；如果需要改变，必须重新获得用户明确确认。协作文档和新提示词不要重复抄写密钥值，只引用其位置。

### 4.4 设备扫描字段

设备结果项包含：

- Device Name / 设备名称
- Risk Level / 风险等级
- Distance / 距离
- Suspicious / 可疑标签
- Device Type / 设备类型

当前示例：手机、电脑、通用设备；只有电脑行进入距离检测；距离示例为 `0.56m`。

### 4.5 电子档案字段与限制

- 档案名称：1–50 个中英文字符或数字。
- 类型预设：身份证、护照、签证。
- 图片来源：相册或相机。
- 格式：JPG、PNG。
- 单张上限：20 MB。
- 单档案最多：5 张图片。
- 操作：新增、查看、保存到相册、重命名、删除。
- 当前数据仅在前端运行期维护，刷新页面后不保证持久化。

### 4.6 会员与恢复购买

- 套餐：周会员、月会员、季会员、年会员。
- 支付：Apple 支付原型流程。
- 恢复购买：验证 Apple ID 购买记录。
- 已注销用户：不能恢复购买。
- 新套餐：覆盖旧会员套餐并更新会员数据。

价格如需修改，应先核对 `内购产品价格.xlsx` 和原始充值页面，不得自行编造。

### 4.7 需求说明 Markdown 格式

`需求说明.md` 当前包含 36 个模块。每个模块必须使用唯一英文键，并保留七个固定字段：

```markdown
## homeRisk
- 标题: 目的地风险入口
- 模块说明: 进入目的地风险查询。
- 数据来源: 本地导航配置。
- 前置条件: 首页已加载。
- 后置结果: 进入风险查询页。
- 显示数量限制: 单个模块
- 排序规则: -
```

限制：

- 不得修改或重复 `##` 后的英文键。
- 不得更改七个字段名。
- 字段保持单行 `- 字段名: 内容` 格式。
- 未定义时使用 `-`。
- HTML 只接收已知模块键和已知字段。
- Markdown 加载失败时，页面回退到 HTML 内置中文说明。

---

## 5. 已确认的设计与交互决定

### 5.1 总体布局

- 桌面端左侧为项目树，右侧为双语手机原型或文档页。
- 英文原型在左，中文原型在右。
- 手机按 6.3 英寸 iPhone 17 比例展示：外框约 `418 × 890 CSS px`，内容屏约 `402 × 874 CSS px`。
- 小屏幕下只展示单列手机原型，桌面项目树隐藏。

### 5.2 双语规则

- 产品原型内的英文内容只出现在英文手机。
- 中文内容只出现在中文手机。
- 两侧主要交互保持同步能力。
- 中文原型显示需求说明圆点；英文原型不显示需求说明圆点。

### 5.3 需求说明交互

- 每个小模块可拥有独立需求圆点。
- 默认只显示带波纹效果的小圆点。
- 点击后在原型外侧的右栏展示说明。
- 说明栏为非模态，不遮挡手机原型，不使用背景蒙层。
- 说明字段：模块说明、数据来源、前置条件、后置结果、显示数量限制、排序规则。
- 底部补充描述已经删除，不得恢复。
- 需求说明只保留中文原型。

### 5.4 流程图

- 三个流程图均为 `index.html` 中的 HTML/CSS 原生绘制。
- 不使用 iframe，不直接引用原 Axure 流程图页面。
- 判断节点必须使用菱形。
- `是/否` 标签不得与节点或连线重叠。
- 流程图不得设置内部滚动条。
- 图过宽或过高时，让浏览器页面本身滚动。
- 风险流程图保留模型配置、返回字段、约束规则和明文 API Key。
- 三个流程图标题下不显示解释性描述句。

### 5.5 已删除内容

以下内容不得恢复：

- 定位天气。
- 暗黑模式入口及相关主题切换。
- “Be ready. Stay safe.” 等宣传文案。
- 首页 Safety brief / Pre-trip checklist 模块。
- 绑定手机号设置行。
- 用户资料头部的手机号。
- 目的地查询和附近资源模块上方的小型全大写引导文案。
- 需求说明栏底部补充描述。
- 英文原型的需求说明圆点。

### 5.6 当前保留内容

- 用户昵称显示为英文 `User nickname*****`，中文 `用户昵称*****`。
- 账户 ID 可以保留。
- 更新日志只保留一条：`2026/9/16 · 1.0.0 · 新增 · 新增 1.0.0 原型。`
- 风险查询结果当前包含温哥华示例；这是后续根据原文件恢复的当前状态。
- 充值/订阅为单独页面。
- 电子档案新增按钮为右下角固定加号图标。

---

## 6. 当前技术实现

### 6.1 技术栈

- 单文件 HTML、CSS、原生 JavaScript。
- 无 npm、无框架、无打包器、无服务端。
- 本地图标资源位于 `assets/icons/`。
- 页面通过复制英文 DOM 并替换文字生成中文手机原型。

### 6.2 核心状态与交互

- `navigate(name)`：切换产品原型页面。
- `showDocument(name)`：切换更新日志、需求列表和流程图。
- `moduleNoteCatalog`：HTML 内置的双语需求说明回退数据。
- `loadModuleNotesMarkdown()`：读取并解析 `需求说明.md`。
- `addModuleNotes()`：只向中文原型挂载需求圆点。
- `vaultRecords`：电子档案运行期数据。
- `selectedFilesByRoot`：上传文件的运行期选择状态。
- `resourceData`：附近资源示例。
- 风险、扫描、注销和会员流程使用本地定时器与前端状态模拟。

### 6.3 GitHub Pages 兼容

- 已增加空文件 `.nojekyll`，避免 Jekyll 转换 `需求说明.md`。
- HTML 使用相对路径读取 Markdown 和 SVG 图标，兼容项目站点子路径。
- 修改 `需求说明.md` 后需要提交并推送，GitHub Pages 完成重新部署后才会生效。
- 尚未在本项目中配置具体 GitHub 仓库、Pages 分支或自定义域名。

### 6.4 验证方法

JavaScript 语法检查：

```bash
node --check <(sed -n '/<script>/,/<\/script>/p' '摄像头监测原型/index.html' | sed '1d;$d')
```

需求说明字段完整性：

```bash
awk '/^## /{if(section && fields!=7){print section ": " fields}; section=$2; fields=0; next} /^- (标题|模块说明|数据来源|前置条件|后置结果|显示数量限制|排序规则):/{fields++} END{if(section && fields!=7){print section ": " fields}}' '摄像头监测原型/需求说明.md'
```

浏览器验收至少覆盖：

- `#home`
- `#risk`
- `#risk-result`
- `#scan-results`
- `#vault`
- `#recharge`
- `#profile`
- `#account-delete-read`
- `#doc-flow-risk`
- `#doc-flow-restore`
- `#doc-flow-subscribe`

---

## 7. 当前进度

### 7.1 已完成

- 新建并持续维护 `摄像头监测原型/` 静态原型。
- 左侧项目树美化、分组、搜索、折叠和页面导航。
- 更新日志和两版需求列表。
- 双语首页及全部主要子页面。
- 6.3 英寸 iPhone 17 尺寸适配。
- 目的地风险查询、查询中、结果页。
- 住宿检测、扫描结果和距离检测。
- 附近医院/警察局资源切换。
- 电子档案新增、上传、预览、重命名和删除交互。
- 会员订阅/充值独立页面及恢复购买入口。
- 我的页面和两步注销账号流程。
- 三个原生流程图及菱形判断节点。
- 流程图内部滚动条已取消，改为浏览器级滚动。
- 中文模块需求圆点和原型外部说明栏。
- `需求说明.md` 外部可编辑数据源。
- GitHub Pages `.nojekyll` 兼容准备。

### 7.2 尚未完成或待确认

- 尚未实际创建或配置 GitHub Pages 发布仓库。
- 尚未确定 Pages 使用仓库根目录还是 `/docs` 目录发布。
- 当前压缩包 `摄像头监测原型.zip` 可能不是最新版本，正式交付前需要重新生成。
- 产品正式英文名已确认为 `TripScope`，会员名称使用 `TripScope Plus`；中文原型也直接使用英文品牌名。
- 风险查询仍是静态模拟，没有真实接口调用、异常处理或网络重试。
- 电子档案数据没有持久化，刷新页面会丢失运行期修改。
- 商店图已有完整文档页，包含 5 张宣传图和 3 张引导图。
- 隐私政策、用户协议主要为入口原型，没有完整正文。
- 需要在真实 GitHub Pages URL 上做一次最终回归测试。

### 7.3 已知历史文档冲突

`design-qa.md` 按时间累积，部分旧段落已经被后续决定覆盖，例如：

- 旧记录曾写流程图直接引用原 HTML，当前已经改为原生重绘。
- 旧记录曾写 API Key 已隐藏，当前按照用户要求保留明文。
- 旧记录曾写英中两侧都有需求点，当前只保留中文需求点。
- 旧记录曾写需求说明使用模态框，当前已改为原型外部非模态栏。
- 旧记录曾写流程图容器内部滚动，当前已改为浏览器级滚动。

后续窗口必须以本手册和当前代码为准，不得仅凭旧 QA 段落修改实现。

---

## 8. 用户工作方式与偏好

- 优先根据原文件复刻字段、内容和流程，不要自行发挥。
- 可以自主优化视觉样式，但不能擅自改变产品含义、字段或流程。
- 如果发现属于自主发挥的内容，应主动列出，由用户决定是否保留。
- 用户通常先要求方案，再确认执行；一旦确认，应直接实施并验证。
- 输出应简洁、直达结果，避免只给泛泛建议。
- 修改应保留现有文件和已确认成果，不重新从零覆盖。
- 中文原型用于需求说明，英文原型用于产品英文内容对照。
- 需求说明需要可由产品人员直接编辑，不应每次依赖代码修改。
- 交付应兼顾本地静态预览和 GitHub Pages 在线访问。

---

## 9. 所有后续窗口必须遵守的全局规则

1. 项目目标不可擅自改变：继续完善可交互、可对照、可静态发布的产品原型。
2. 最新明确用户要求优先；原文件其次；当前已确认实现再次之；旧 QA 只用于追溯。
3. 不随意重构整个 `index.html`，优先做局部、可验证的修改。
4. 保持以下关键名称一致：`index.html`、`需求说明.md`、屏幕 `data-screen`、文档 `data-doc-page`、需求模块英文键。
5. 不改变 `原型文件/`；该目录是源资料，不是实现目录。
6. 不删除用户文件，不覆盖无关内容，不重建整个项目目录。
7. 修改代码前先说明影响范围；修改后必须验证并更新进度。
8. 影响字段、流程、模型配置、产品文案或页面结构的重要决定必须记录原因。
9. 不确定的内容标记为“待确认”，不得把推测写成确定产品规则。
10. 价格、字段、限制、接口参数优先从原文件或现有资料读取，不得编造。
11. 英文和中文产品交互必须保持一致；需求说明只挂载在中文原型。
12. 需求说明默认隐藏，仅显示圆点；点击后在原型外部说明栏展示。
13. 流程图继续使用原生 HTML/CSS，判断节点使用菱形，不允许内部滚动条。
14. 定位天气、暗黑模式、绑定手机号、宣传文案、Safety brief 和已删除说明不得恢复。
15. 明文 API Key 当前保持不动；任何安全整改必须先获得用户新的明确授权。
16. 不把 `file://` 作为外部 Markdown 验收方式；使用本地 HTTP 或 HTTPS。
17. GitHub Pages 发布目录必须保留 `.nojekyll` 和 `assets/icons/` 相对结构。
18. 页面功能应支持键盘、清晰焦点、语义按钮、ARIA 标签和减少动效偏好。
19. 视觉调整不得遮挡内容、截断字段或引入不必要的嵌套滚动。
20. 每次任务完成后报告：修改文件、完成内容、验证结果、剩余问题、是否需要其他窗口同步。

---

## 10. 分窗口方案

### 窗口 A：项目统筹与变更控制

负责：

- 接收用户需求并判断影响范围。
- 维护优先级、决策和跨窗口依赖。
- 更新本手册的当前进度、决定和未完成事项。
- 指定下一步应交给哪个窗口。

需要读取：

- 本手册全部内容。
- `跨窗口通知记录.md`。
- 其他窗口的完成报告。

不能越界：

- 不直接大规模修改产品代码。
- 不替代原文件溯源窗口猜测字段。
- 不未经用户确认修改已锁定决定。

期望输出：变更计划、影响范围、任务拆分、决定记录和执行顺序。

依赖：所有窗口向其发送重要变更通知；它负责合并冲突。

### 窗口 B：原文件溯源与需求核对

负责：

- 在 `原型文件/`、RP 文件说明和表格资料中查找原始内容。
- 对比原文件和当前原型，列出一致项、缺失项和自主发挥项。
- 输出字段、流程、文案和约束的证据位置。

需要读取：

- 本手册第 2、4、5、7 节。
- 用户指定的原始 HTML 或表格。
- 当前 `index.html` 对应页面。

不能越界：

- 默认只核对，不直接改代码。
- 不用自己的产品判断补齐原文件缺失内容。
- 不把旧页面中已被用户明确删除的功能恢复为当前需求。

期望输出：来源文件、字段清单、流程节点、差异表、待确认问题。

依赖：向窗口 A 和窗口 C 发送核对结论。

### 窗口 C：HTML/CSS/JavaScript 原型实现

负责：

- 在 `摄像头监测原型/index.html` 中实现已确认修改。
- 维护双语页面、交互状态、响应式、流程图和资源路径。
- 做语法检查和浏览器回归。

需要读取：

- 本手册全部全局规则。
- 窗口 A 的明确任务单。
- 窗口 B 的原文件核对结果。
- `原型导航与需求说明复用规范.md`。

不能越界：

- 不修改 `原型文件/`。
- 不自主增加字段、页面、营销文案或业务规则。
- 不修改需求说明 Markdown 的产品内容，除非任务明确要求。
- 不隐藏或删除明文 API Key。

期望输出：修改位置、交互说明、验证结果和风险。

依赖：完成后通知窗口 D 做内容同步，通知窗口 E 做 QA。

### 窗口 D：需求说明与产品文案维护

负责：

- 编辑 `需求说明.md` 的中文模块内容。
- 保证 36 个模块键和七字段格式完整。
- 根据已确认页面变化补充数据来源、前后置条件、数量限制和排序规则。

需要读取：

- 本手册第 4.7、5.3、9 节。
- `需求说明.md`。
- `index.html` 中的 `moduleNoteTargets` 和 `moduleNoteCatalog`。

不能越界：

- 不修改模块英文键和字段名。
- 不修改英文原型或为英文原型增加需求点。
- 不在没有来源时编造业务限制。
- 不直接重构页面代码；发现挂载缺失时通知窗口 C。

期望输出：变更模块、字段变更、来源依据、格式检查结果。

依赖：页面模块发生增删时从窗口 C 接收通知；内容变更后通知窗口 E 验证。

### 窗口 E：视觉、交互与回归验收

负责：

- 使用浏览器检查英文/中文、桌面/小屏和关键交互。
- 验证原文件一致性、可访问性、内容完整性和滚动行为。
- 维护新的 QA 结论，但不得让旧结论覆盖当前状态。

需要读取：

- 本手册第 3、5、6.4、7.3、9 节。
- 当前 `index.html` 和 `需求说明.md`。
- 本轮修改对应的参考截图或原文件。

不能越界：

- 默认只检查和报告，不直接改变产品决定。
- 不把审美偏好当作 P0/P1 问题。
- 不忽略用户明确要求的浏览器级横向滚动。

期望输出：验收页面、操作步骤、通过项、问题优先级、截图或证据。

依赖：接收窗口 C、D 的完成通知；问题返回对应窗口修复。

### 窗口 F：GitHub Pages 发布与交付

负责：

- 整理发布目录和仓库结构。
- 配置 GitHub Pages 发布源。
- 验证 HTTPS 下的 Markdown、图标和哈希页面。
- 生成最新交付压缩包。

需要读取：

- 本手册第 2.2、2.3、6.3、7.2、9 节。
- `.nojekyll`、`index.html`、`需求说明.md` 和 `assets/`。

不能越界：

- 不修改产品逻辑或视觉。
- 不自行删除明文 API Key；只能再次提示当前公开风险。
- 不上传 `.DS_Store`、临时截图或过期压缩包。
- 未获授权时不创建远程仓库、推送或改变 Pages 设置。

期望输出：发布结构、线上地址、部署状态、资源检查结果、回滚方式。

依赖：必须在窗口 E 验收通过后执行。

---

## 11. 每个窗口可直接复制的新对话提示词

### 11.1 窗口 A：项目统筹与变更控制

```text
你是“摄像头监测原型”项目的项目统筹与变更控制窗口。

项目背景：这是从 Axure 原文件整理出的纯静态双语交互原型，当前实现位于“摄像头监测原型/”。项目要求忠实原文件、保留已确认结果、支持本地 HTTP 和 GitHub Pages。

开始前必须完整阅读：
1. 摄像头监测原型/项目交接与分窗口协作手册.md
2. 摄像头监测原型/跨窗口通知记录.md

你的具体任务：接收本轮需求，判断它影响哪些文件、页面、字段和其他窗口；形成执行计划、优先级、依赖和验收标准。必要时把任务拆给原文件核对、代码实现、需求文案、QA 或发布窗口。

全局规则：严格遵守协作手册第 9 节；最新用户确认优先；不得自行改变字段、流程和已锁定决定；不直接大规模修改代码。

输入资料：用户本轮要求、当前协作手册、跨窗口通知和相关窗口结果。

输出格式：
- 需求结论
- 影响范围
- 需要执行的窗口及顺序
- 每个窗口的明确任务
- 验收标准
- 待确认问题
- 需要写入跨窗口通知的决定

协作方式：任何跨窗口影响都使用手册中的【跨窗口通知】模板；收到其他窗口通知后，先更新项目判断，再安排下一步。
```

### 11.2 窗口 B：原文件溯源与需求核对

```text
你是“摄像头监测原型”项目的原文件溯源与需求核对窗口。

项目背景：当前原型来自“原型文件/”中的 Axure 导出页面。用户要求产品字段、内容和流程按原文件实现，不允许无依据发挥；但最新明确删除和修改要求优先于旧原文件。

开始前必须完整阅读：
1. 摄像头监测原型/项目交接与分窗口协作手册.md
2. 与本任务相关的原型文件/*.html
3. 摄像头监测原型/index.html 中对应页面

你的具体任务：针对当前指定模块，查清原始字段、文案、流程节点、前后置条件和限制，并与当前实现逐项比较。

全局规则：只做证据核对，不直接改代码；不恢复已明确删除的内容；不把自己的产品建议伪装成原文件要求；找不到依据时写“原文件未定义”。

输入资料：用户指定页面/截图、原文件、当前 index.html、必要时的表格资料。

输出格式：
- 原始来源文件和位置
- 原始页面/状态
- 字段与文案清单
- 流程节点及分支
- 当前实现一致项
- 缺失或偏差
- 当前实现中可能属于自主发挥的内容
- 待用户确认项

协作方式：完成后生成【跨窗口通知】，接收窗口为“项目统筹”和“原型实现”，明确哪些内容可实施、哪些仍待确认。
```

### 11.3 窗口 C：HTML/CSS/JavaScript 原型实现

```text
你是“摄像头监测原型”项目的 HTML/CSS/JavaScript 原型实现窗口。

项目背景：项目是单文件原生 HTML/CSS/JavaScript 双语静态原型，主文件为“摄像头监测原型/index.html”，原始依据在“原型文件/”，中文模块说明在“需求说明.md”。

开始前必须完整阅读：
1. 摄像头监测原型/项目交接与分窗口协作手册.md
2. 摄像头监测原型/原型导航与需求说明复用规范.md
3. 项目统筹窗口发来的本轮任务单
4. 原文件核对窗口发来的证据结论

你的具体任务：只实施已经确认的页面、样式或交互修改，并保持英文/中文产品交互一致。

全局规则：严格遵守手册第 9 节；不修改“原型文件/”；不随意重构整个 index.html；不增加无来源字段；需求点只保留中文；流程图不得使用内部滚动条；明文 API Key 不得自行处理。

输入资料：任务单、原文件核对结论、参考截图、当前代码。

输出格式：
- 修改文件和位置
- 实现内容
- 对其他页面/窗口的影响
- 语法检查结果
- 浏览器验证页面和结果
- 剩余风险或待确认项
- 跨窗口通知

协作方式：完成后分别通知“需求说明窗口”是否需要同步模块内容，通知“QA 窗口”应重点验证哪些页面和状态。
```

### 11.4 窗口 D：需求说明与产品文案维护

```text
你是“摄像头监测原型”项目的需求说明与产品文案维护窗口。

项目背景：中文原型的需求圆点从“摄像头监测原型/需求说明.md”读取。该 Markdown 是产品人员可直接编辑的说明数据源；英文原型不显示需求圆点。

开始前必须完整阅读：
1. 摄像头监测原型/项目交接与分窗口协作手册.md
2. 摄像头监测原型/需求说明.md
3. 摄像头监测原型/index.html 中 moduleNoteTargets、moduleNoteCatalog 和 Markdown 解析部分

你的具体任务：根据已经确认的页面变化，更新相关模块的标题、模块说明、数据来源、前置条件、后置结果、显示数量限制和排序规则。

全局规则：不得改模块英文键，不得改七个字段名，不得增加英文原型说明，不得无来源编造限制；不适用时使用“-”。

输入资料：原型实现窗口的变更通知、原文件核对结论、当前需求说明文档。

输出格式：
- 修改模块键
- 每个字段的旧值与新值
- 内容来源
- 是否存在待确认字段
- 36 模块和七字段完整性检查结果
- 跨窗口通知

协作方式：如果页面出现没有映射的模块，只通知“原型实现窗口”补充挂载，不自行重构 index.html；完成后通知“QA 窗口”检查圆点、标题和外部说明栏。
```

### 11.5 窗口 E：视觉、交互与回归验收

```text
你是“摄像头监测原型”项目的视觉、交互与回归验收窗口。

项目背景：项目为左右项目树 + 英中双语手机原型/文档页的静态网站。当前核心要求是内容忠实、双语交互一致、中文需求说明可外部编辑、流程图完整且只使用浏览器级滚动。

开始前必须完整阅读：
1. 摄像头监测原型/项目交接与分窗口协作手册.md
2. 本轮代码/需求说明窗口的完成通知
3. 用户参考截图或对应原文件

你的具体任务：在 http://localhost:8766/摄像头监测原型/ 下验证本轮变化，并执行关键页面回归。

全局规则：默认只检查不改产品决定；不得根据旧 QA 回滚当前要求；浏览器级滚动是流程图的明确要求；问题必须给出页面、操作、预期和实际结果。

输入资料：本轮变更说明、参考图、当前浏览器页面。

输出格式：
- 测试环境和 URL
- 验证页面/状态
- 通过项
- 问题清单（P0/P1/P2/P3）
- 双语一致性
- 需求说明加载与交互
- 响应式、滚动和可访问性
- 最终结论
- 需要发给对应窗口的跨窗口通知

协作方式：问题按归属发给“原型实现窗口”或“需求说明窗口”；全部通过后通知“项目统筹”和“发布窗口”。
```

### 11.6 窗口 F：GitHub Pages 发布与交付

```text
你是“摄像头监测原型”项目的 GitHub Pages 发布与交付窗口。

项目背景：项目是无构建步骤的纯静态网站。发布目录包含 index.html、需求说明.md、.nojekyll、说明文档和 assets/icons。页面使用相对路径并通过 fetch 读取 Markdown。

开始前必须完整阅读：
1. 摄像头监测原型/项目交接与分窗口协作手册.md
2. QA 窗口的最终通过通知
3. 当前发布目录结构

你的具体任务：根据用户选择，将发布文件整理到仓库根目录或 /docs，配置并验证 GitHub Pages，最后生成最新交付压缩包。

全局规则：未获授权不得创建远程仓库或推送；不得修改产品逻辑；保留 .nojekyll；不要上传 .DS_Store；明文 API Key 按当前决定保留，但发布前必须再次提示它将公开可见。

输入资料：用户的 GitHub 仓库信息、发布分支选择、QA 通过结论和当前项目目录。

输出格式：
- 发布目录结构
- 实际修改和配置
- GitHub Pages 地址
- Markdown、SVG、哈希路由验证
- 部署状态与更新时间
- 最新压缩包位置
- 已知公开风险
- 回滚方式

协作方式：发现产品文件问题时停止发布并通知“项目统筹”；线上验证通过后向所有窗口发送最终发布通知。
```

---

## 12. 跨窗口同步机制

任何会影响其他窗口的字段、流程、文件名、页面标识、模块键、模型方案、用户决定或测试结论，都必须记录在 `跨窗口通知记录.md`，并使用以下格式：

```text
【跨窗口通知】
发送窗口：
接收窗口：
影响范围：
具体变更：
变更原因：
接收方需要采取的动作：
是否阻塞对方任务：是 / 否
相关文件 / 字段 / 模型 / 实验编号：
验证结果：
待确认事项：
日期：
```

接收方处理规则：

1. 先确认通知是否与自己当前任务冲突。
2. 把确认后的变更写入自己的任务计划。
3. 如果通知阻塞当前工作，停止相关修改并返回项目统筹窗口。
4. 完成接收动作后，在同一通知下补充“接收结果”。
5. 未写通知的口头变更不能自动视为全项目共识。

必须通知的典型情况：

- 新增或删除 `data-screen` 页面。
- 新增或删除 `data-doc-page` 文档。
- 新增、删除或改名需求模块键。
- 修改模型字段、接口参数或 JSON 结构。
- 修改会员价格、图片数量、字符长度等业务限制。
- 修改英文/中文同步规则。
- 修改流程图节点或分支。
- 修改 GitHub Pages 发布目录。
- 用户确认保留或删除某个争议内容。

---

## 13. 下一步计划与优先级

### P0：发布前必须处理

1. 用户确定 GitHub 仓库、发布分支和根目录或 `/docs` 方案。
2. 在正式 Pages URL 上验证 `需求说明.md` 能被原样读取。
3. 验证所有 SVG 图标和哈希页面路径。
4. 再次向用户明确明文 API Key 会公开；按用户最终决定执行。
5. 重新生成最新 `摄像头监测原型.zip`，避免交付旧版本。

### P1：建议尽快确认

1. 上架前在 App Store Connect 验证 `TripScope` 名称可用性，并统一商店元数据、图标和截图中的品牌名。
2. 决定风险查询是否继续使用静态示例，还是实现真实 API 接入。
3. 决定电子档案是否需要浏览器本地持久化。
4. 补充隐私政策、用户协议和商店图的完整展示页。

### P2：质量完善

1. 清理或重写 `design-qa.md` 中已经过时的历史结论，或给旧段落加“已覆盖”标记。
2. 增加简单的自动化页面冒烟检查。
3. 在 GitHub Pages、桌面浏览器和手机浏览器分别检查响应式。
4. 给发布仓库增加 `.gitignore`，排除 `.DS_Store` 和临时文件。

推荐执行顺序：

```text
窗口 A 确认发布范围
→ 窗口 B 核对仍有争议的原文件内容
→ 窗口 C/D 完成最后修改
→ 窗口 E 全量回归
→ 窗口 F 发布 GitHub Pages 并生成最新压缩包
```

---

## 14. 新窗口快速接手清单

新窗口开始工作前，至少回答以下问题：

- 我负责哪个窗口角色？
- 我是否已完整阅读本手册？
- 当前任务的来源是用户最新要求、原文件还是历史记录？
- 是否存在会覆盖旧决定的新确认？
- 我要修改哪些文件，不能修改哪些文件？
- 是否影响双语同步、需求说明或流程图？
- 是否需要发送跨窗口通知？
- 完成后如何验证？

如果上述问题无法回答，不应直接修改项目。
