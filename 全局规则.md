# 摄像头监测原型：全局规则

> 本文件是所有后续对话窗口必须共同遵守的统一规则。  
> 详细项目背景、窗口职责和提示词见 `项目交接与分窗口协作手册.md`。  
> 最新明确用户要求始终高于本文件中的历史描述；发生冲突时必须记录原因并发送跨窗口通知。

## 一、目标与依据

1. 项目目标不可擅自改变：持续完善可交互、可中英对照、可静态发布的摄像头监测与旅行安全产品原型。
2. 内容依据优先级：最新用户确认 → 当前已确认实现 → `原型文件/` → `需求说明.md` → 历史 QA 记录。
3. 用户明确删除或修改的内容，不得因为原文件仍然存在而自动恢复。
4. 找不到来源的字段、流程、限制或文案必须标记为“待确认”，不得写成确定规则。
5. 视觉样式可以优化，但产品含义、字段、页面关系和业务流程不能自行发挥。

## 二、文件与修改范围

6. 当前实现目录为 `摄像头监测原型/`；`原型文件/` 是只读来源，不得修改。
7. 主实现文件为 `index.html`，中文需求说明数据源为 `需求说明.md`。
8. 不随意重构或整体覆盖 `index.html`，优先进行局部、可回退、可验证的修改。
9. 不删除用户文件，不覆盖无关内容，不重新创建另一套重复原型。
10. 保持文件名、`data-screen`、`data-doc-page` 和需求模块英文键稳定；如确需更名，必须同步通知相关窗口。
11. `.nojekyll` 和 `assets/icons/` 目录结构必须保留，以支持 GitHub Pages。
12. `.DS_Store`、临时截图、旧压缩包和本地缓存不属于发布内容。

## 三、产品内容规则

13. 英文产品内容只出现在英文手机，中文产品内容只出现在中文手机。
14. 英文和中文产品原型的主要页面、状态和交互必须保持一致。
15. 需求说明圆点只保留在中文原型，英文原型不得重新增加需求点。
16. 用户昵称保持为英文 `User nickname*****`、中文 `用户昵称*****`；用户资料头部不显示手机号。
17. 绑定手机号、定位天气、暗黑模式、宣传文案、Safety brief 和已删除的需求说明底部文案不得恢复。
18. 更新日志当前只保留一条：`2026/9/16 · 1.0.0 · 新增 · 新增 1.0.0 原型。`
19. 温哥华风险示例是后续根据原文件恢复的当前状态，除非用户再次要求，否则不得删除。
20. 价格、字段、数量限制、接口参数和流程节点必须优先从原文件或现有资料读取，不得编造。

## 四、需求说明规则

21. `需求说明.md` 是中文模块说明的外部编辑数据源。
22. 每个模块必须保留唯一的 `## 英文键`，不得修改或重复。
23. 每个模块必须保留七个字段：标题、模块说明、数据来源、前置条件、后置结果、显示数量限制、排序规则。
24. 字段必须使用单行 `- 字段名: 内容` 格式；无特殊内容时填写 `-`。
25. 需求说明默认隐藏，只显示带波纹效果的圆点。
26. 点击圆点后，说明显示在原型外部的非模态右栏中，不遮挡手机原型，不使用背景蒙层。
27. 需求说明栏不得恢复已删除的底部补充描述。
28. 新增或删除页面模块时，必须同步检查 `moduleNoteCatalog`、`moduleNoteTargets` 和 `需求说明.md`。
29. Markdown 加载失败时保留 HTML 内置说明作为回退，不得让页面报错或失去交互。

## 五、流程图规则

30. 风险查询、恢复购买、订阅会员三个流程图使用 `index.html` 内的原生 HTML/CSS 绘制。
31. 不使用 iframe，不直接嵌入或引用原 Axure 流程图页面。
32. 判断节点必须使用菱形；`是/否` 标签、连线和节点文字不得遮挡或重叠。
33. 流程图不得设置内部横向或纵向滚动条；内容超出视口时使用浏览器页面滚动条。
34. 三个流程图标题下不增加解释性描述句。
35. 风险流程图保留模型配置、固定 JSON 字段、约束规则和当前明文 API Key。
36. 用户已明确要求不处理明文 API Key；任何隐藏、替换、删除或密钥整改都必须重新获得明确授权。
37. 协作文档和新窗口提示词只引用密钥所在位置，不重复抄写密钥值。

## 六、交互、响应式与可访问性

38. 手机原型以 6.3 英寸 iPhone 17 比例为当前尺寸依据。
39. 页面不得因固定高度或 `overflow:hidden` 截断重要内容。
40. 避免不必要的嵌套滚动；固定导航和操作区不能遮挡可滚动内容。
41. 交互按钮必须使用语义元素，并保留清晰焦点、可理解标签和键盘操作能力。
42. 图标优先使用本地 SVG，不使用运行时网络依赖或表情符号充当结构图标。
43. 功能状态不能只依赖颜色表达，应同时使用文字、形状或图标。
44. 动效必须遵守 `prefers-reduced-motion`，不能阻断主要交互。
45. 英中双栏、桌面、小屏和横向溢出场景都需要在修改后检查。

## 七、本地预览与发布

46. 外部 Markdown 验收必须使用 HTTP 或 HTTPS，不能使用 `file://`。
47. 本地标准地址为 `http://localhost:8766/摄像头监测原型/`。
48. GitHub Pages 发布目录根部必须保留空文件 `.nojekyll`，避免 Markdown 被 Jekyll 转换。
49. 所有资源继续使用相对路径，不能写入本机绝对路径或只在 localhost 有效的资源地址。
50. 未经用户授权，不创建远程仓库、不推送代码、不修改 GitHub Pages 设置。
51. 正式发布前必须再次提示：当前流程图中的明文 API Key 将随 GitHub Pages 公开。
52. 发布前需要重新生成最新压缩包，不能默认使用工作区中已有的旧 ZIP。

## 八、执行与验证

53. 修改前说明目标、范围和可能影响的其他窗口。
54. 代码修改后至少执行 JavaScript 语法检查，并在相关哈希页面进行浏览器验证。
55. `需求说明.md` 修改后必须检查模块数量和七字段完整性。
56. 流程图修改后必须分别检查三个流程图，不能只验证其中一个。
57. 页面模块修改后必须检查英文、中文两侧的内容和交互同步。
58. 重要决定必须记录原因；不确定内容必须明确标记为“待确认”。
59. 每次完成任务后必须报告：修改文件、完成内容、验证结果、剩余问题、是否需要跨窗口同步。
60. 不把“已经写入代码”等同于“已经完成”；只有通过相应验证后才能标记完成。

## 九、跨窗口协作

61. 影响其他窗口的页面、字段、流程、模块键、模型、发布结构或用户决定，必须写入 `跨窗口通知记录.md`。
62. 通知必须包含发送窗口、接收窗口、影响范围、具体变更、原因、接收动作、阻塞状态、相关文件和验证结果。
63. 接收窗口必须把已确认变更写入自己的任务计划；发生冲突时返回项目统筹窗口处理。
64. 未写入通知且未经确认的口头推测，不能自动视为全项目决定。
65. `design-qa.md` 是累计历史，不是当前全局规则；其中与本文件或当前代码冲突的旧结论均视为已覆盖。

## 十、完成定义

一项任务只有同时满足以下条件才算完成：

- 内容符合最新用户要求和已确认来源。
- 没有恢复被明确删除的内容。
- 没有破坏英中同步、需求说明、流程图或发布结构。
- 已执行与改动风险相匹配的语法和浏览器验证。
- 已记录待确认事项和跨窗口影响。
- 已向用户给出简洁、可核查的完成说明。
