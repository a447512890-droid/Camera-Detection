# 跨窗口通知记录

本文件记录会影响其他窗口的页面、字段、流程、文件、模型、发布和用户决定。

使用规则：

- 新通知追加在文件顶部“通知记录”下方。
- 接收窗口完成同步后补充“接收结果”。
- 未确认内容必须标记为“待确认”，不能作为全局决定执行。
- 通知格式和全局规则见 `项目交接与分窗口协作手册.md`。

---

## 通知记录

### 2026-09-17 · 修复中文注销页按钮位置

【跨窗口通知】

- 发送窗口：HTML/CSS/JavaScript 原型实现窗口
- 接收窗口：项目统筹、需求说明、QA 窗口
- 影响范围：中文原型“注销账号－账号注销协议”页
- 具体变更：需求说明标记由包裹整个底部定位容器调整为仅包裹“同意并继续”按钮，避免改变绝对定位的参照元素。
- 变更原因：用户反馈中文按钮错误叠加在协议正文中。
- 接收方需要采取的动作：QA 窗口回归中英文按钮底部位置、10 秒倒计时和启用后的点击跳转。
- 是否阻塞对方任务：否
- 相关文件 / 字段 / 模型 / 实验编号：`index.html`、`account-delete-read`、`deletionAction`
- 验证结果：JavaScript 语法检查通过；浏览器确认中英文按钮均固定在页面底部并保留 24px 间距，10 秒倒计时同步结束后均正常启用；中文按钮可进入最终注销确认页。
- 待确认事项：无
- 日期：2026-09-17
- 接收结果：待后续窗口填写。

### 2026-09-17 · 放大订阅页说明文字

【跨窗口通知】

- 发送窗口：HTML/CSS/JavaScript 原型实现窗口
- 接收窗口：项目统筹、需求说明、QA 窗口
- 影响范围：中英文订阅会员页的套餐卡、恢复购买、订阅说明与底部协议区
- 具体变更：放大套餐名称与首周价格说明、恢复购买提示、订阅说明正文、试用提示、法律说明和协议链接；同步增加套餐卡高度与页面底部留白，避免大字号内容被订阅浮层遮挡。
- 变更原因：用户反馈原字号过小、看不清。
- 接收方需要采取的动作：QA 窗口回归中英文订阅页的可读性、换行和滚动；需求说明窗口继续控制订阅说明篇幅。
- 是否阻塞对方任务：否
- 相关文件 / 字段 / 模型 / 实验编号：`index.html`、`recharge`、`recharge-bar`
- 验证结果：JavaScript 语法检查通过；浏览器确认目标字号分别提升至 9–13px，中英文套餐卡无横向溢出；滚动到底后订阅说明与底部浮层分别保留约 50px、77px 间距，内容无遮挡。
- 待确认事项：无
- 日期：2026-09-17
- 接收结果：待后续窗口填写。

### 2026-09-17 · 删除需求列表快捷跳转列

【跨窗口通知】

- 发送窗口：HTML/CSS/JavaScript 原型实现窗口
- 接收窗口：项目统筹、需求说明、QA 窗口
- 影响范围：需求列表 v1.0.1 和 v1.0.0 表格结构
- 具体变更：按用户标注删除整个“快捷跳转”列，包括表头、链接文字、横线和自动附加的信息圆点。两张表现均仅保留序号、需求名称、需求描述、备注四列。
- 变更原因：用户要求去掉该列的所有内容。
- 接收方需要采取的动作：QA 窗口回归两个需求列表；需求说明窗口不再依赖需求表信息圆点入口。
- 是否阻塞对方任务：否
- 相关文件 / 字段 / 模型 / 实验编号：`index.html`、`requirements-101`、`requirements-100`
- 验证结果：JavaScript 语法检查通过；浏览器验证两张表均为 4 列，信息圆点数量为 0。
- 待确认事项：无
- 日期：2026-09-17
- 接收结果：待后续窗口填写。

### 2026-09-17 · 新增宣传图与引导图

【跨窗口通知】

- 发送窗口：HTML/CSS/JavaScript 原型实现窗口
- 接收窗口：项目统筹、需求说明、QA、发布窗口
- 影响范围：商店图文档页、发布资源、页面标识
- 具体变更：新增 `data-doc-page="store-artwork"`，将“商店图”占位项改为可浏览与下载页；新增 5 张 1290×2796 宣传图和 3 张同尺寸引导图。宣传图顺序为旅行安全总览、目的地风险、附近求助、电子档案、住宿安全检测；摄像头监测仅作为第 5 张。
- 变更原因：用户要求参考指定产品的宣传图风格，且明确要求摄像头监测只占其中一张。该最新要求覆盖 `App Store元数据.md` 中将住宿检测拆为两张的历史建议。
- 接收方需要采取的动作：需求说明窗口后续更新截图文案规划；QA 窗口检查 `#doc-store-artwork`、顺序、下载链接和响应式；发布窗口将 `assets/generated/` 加入发布目录。
- 是否阻塞对方任务：否
- 相关文件 / 字段 / 模型 / 实验编号：`index.html`、`assets/generated/`、`data-doc-page="store-artwork"`、`App Store元数据.md`
- 验证结果：`index.html` 与导出源文件的 JavaScript 语法检查通过；8 张成图均为 1290×2796 PNG；`#doc-store-artwork` 桌面和 390px 移动端宽度无水平溢出；8 张图片和下载链接均存在；直接访问哈希时“商店图”导航正确选中。
- 待确认事项：无
- 日期：2026-09-17
- 接收结果：待后续窗口填写。

### 2026-09-17 · 新建 TripScope App Store 元数据文档

【跨窗口通知】

- 发送窗口：需求说明与产品文案维护窗口（窗口 D）
- 接收窗口：项目统筹、原型实现、QA、发布窗口
- 影响范围：App Store 商店元数据、截图文案、TripScope Plus 订阅商品文案、审核备注、隐私与权限申报清单
- 具体变更：新建 `App Store元数据.md`，包含简体中文和美式英语元数据、核心宣传语、6 张截图文案规划、4 类订阅商品文案、App Review 备注、URL/隐私/权限待确认清单和 Apple 字段限制。
- 变更原因：用户要求生成 App Store 元数据并保存为独立 Markdown 文件。
- 接收方需要采取的动作：项目统筹窗口补充未确定的法律实体和首发市场；原型实现与 QA 窗口确认实际权限、数据行为和功能描述一致；发布窗口上架前填充 URL、Bundle ID、SKU、联系信息、商品 ID 和最终价格。
- 是否阻塞对方任务：否；未确定字段会阻塞最终上架提交。
- 相关文件 / 字段 / 模型 / 实验编号：`App Store元数据.md`、`index.html`、TripScope Plus 订阅商品
- 验证结果：中英文 App 名称、副标题、推广文本、描述和关键词均在 Apple 字段限制内；中文关键词 89 字节，英文关键词 96 字节。
- 待确认事项：名称可用性、法律实体、Copyright、Bundle ID、SKU、首发地区、年龄分级、全部 URL、App Privacy、联系信息、订阅商品 ID/价格/权益和审核测试路径。
- 日期：2026-09-17
- 接收结果：待各窗口处理。

### 2026-09-17 · 产品英文名确认为 TripScope

【跨窗口通知】

- 发送窗口：需求说明与产品文案维护窗口（窗口 D）
- 接收窗口：项目统筹、原型实现、需求说明、QA、发布窗口
- 影响范围：页面标题、原型可访问名称、会员文案、中英文无障碍名称、项目说明和后续发布物
- 具体变更：用户已确认产品英文名为 `TripScope`，后续应将原型中的 `Safe Me`、`Safe Me Plus` 以及明确指向产品的临时名称统一更新。
- 变更原因：用户确认正式产品英文名。
- 接收方需要采取的动作：原型实现窗口更新 `index.html` 中的品牌文案与 ARIA 名称；项目统筹窗口更新手册中的未确定状态；QA 窗口检查英中展示与旧名遗留；发布窗口在交付物中使用新名称。
- 是否阻塞对方任务：否；但应在下次原型发布前完成名称统一。
- 相关文件 / 字段 / 模型 / 实验编号：`index.html`、`项目交接与分窗口协作手册.md`、后续发布压缩包与 App Store 元数据
- 验证结果：已完成全项目文本检索，已定位 `index.html` 中的页面标题、会员文案、翻译映射和 ARIA 名称。
- 待确认事项：发布时的压缩包是否重命名；实现目录本次保持不变。
- 日期：2026-09-17
- 接收结果：原型实现与需求文案窗口已处理：英中原型均使用 `TripScope`，会员名称使用 `TripScope Plus`；页面标题、项目树、会员文案、翻译映射与 ARIA 名称已统一。JavaScript 语法检查通过，36 个需求模块七字段结构完整，本地 HTTP 下已验证首页及会员入口的英中文显示。待 QA 与发布窗口继续处理。

### 2026-09-17 · 拆分独立全局规则

【跨窗口通知】

- 发送窗口：当前主窗口
- 接收窗口：后续全部窗口
- 影响范围：所有窗口的执行边界、验证和交付
- 具体变更：新增独立的 `全局规则.md`，把目标、文件、产品内容、需求说明、流程图、响应式、发布、验证和跨窗口规则集中为统一入口。
- 变更原因：用户需要明确、可单独引用的全局规则文件。
- 接收方需要采取的动作：开始任务前同时阅读 `项目交接与分窗口协作手册.md` 和 `全局规则.md`。
- 是否阻塞对方任务：否
- 相关文件 / 字段 / 模型 / 实验编号：`全局规则.md`
- 验证结果：已检查规则包含 10 个分类和 65 条编号规则。
- 待确认事项：无
- 接收结果：待后续窗口填写。

### 2026-09-17 · 建立分窗口协作机制

【跨窗口通知】

- 发送窗口：当前主窗口
- 接收窗口：后续全部窗口
- 影响范围：项目管理、原文件核对、原型实现、需求说明、QA、GitHub Pages 发布
- 具体变更：新增 `项目交接与分窗口协作手册.md` 与本通知记录，后续窗口按角色边界工作并记录跨窗口影响。
- 变更原因：当前对话上下文较长，需要形成可直接交接的新窗口项目说明。
- 接收方需要采取的动作：开始任务前完整阅读协作手册；发生跨窗口影响时追加通知。
- 是否阻塞对方任务：否
- 相关文件 / 字段 / 模型 / 实验编号：`项目交接与分窗口协作手册.md`、`跨窗口通知记录.md`
- 验证结果：已确认协作手册包含项目说明、全局规则、6 个窗口方案、6 份可复制提示词、跨窗口机制和执行顺序；通知模板字段完整。
- 待确认事项：无
- 接收结果：待后续窗口填写。

---

## 新通知模板

【跨窗口通知】

- 发送窗口：
- 接收窗口：
- 影响范围：
- 具体变更：
- 变更原因：
- 接收方需要采取的动作：
- 是否阻塞对方任务：是 / 否
- 相关文件 / 字段 / 模型 / 实验编号：
- 验证结果：
- 待确认事项：
- 日期：
- 接收结果：
